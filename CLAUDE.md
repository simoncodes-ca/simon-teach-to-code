# CLAUDE.md

This repo's conventions live in @AGENTS.md — read it before changing anything here.

The rule worth repeating: the empty `// TODO` functions in each project's `.js` are the deliverable, not a bug. Improve the scaffold around them; fill them in only when explicitly asked.
